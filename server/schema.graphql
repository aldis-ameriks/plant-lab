# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

# The javascript `Date` as string. Type represents date and time as the ISO Date string.
scalar DateTime

type Device {
  id: ID!
  name: String
  room: String
  firmware: String
}

type Mutation {
  updateDeviceName(name: String!, deviceId: ID!): Device!
  updateDeviceRoom(room: String!, deviceId: ID!): Device!
  removeDevice(deviceId: ID!): ID!
  addDevice(input: NewDeviceInput!): Device!
  saveReading(input: String!): String!
}

input NewDeviceInput {
  id: Float!
  name: String!
  room: String
  firmware: String!
}

type Query {
  device(deviceId: ID!): Device!
  devices: [Device!]!
  readings(date: String, deviceId: ID!): [Reading!]!
  lastReading(deviceId: ID!): Reading
  lastWateredTime(deviceId: ID!): DateTime
}

type Reading {
  device_id: ID!
  time: DateTime!
  moisture: Float!
  temperature: Float!
  battery_voltage: Float!
  light: Float
}
