# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

# The javascript `Date` as string. Type represents date and time as the ISO Date string.
scalar DateTime

type Device {
  firmware: String
  id: ID!
  name: String
  room: String
  type: DeviceType!
}

# Device type
enum DeviceType {
  hub_10
  sensor_10
}

type Mutation {
  addDevice(input: NewDeviceInput!): Device!
  pairDevice(input: PairDeviceInput!): Boolean!
  removeDevice(deviceId: ID!): ID!
  saveReading(input: String!): String!
  updateDeviceName(deviceId: ID!, name: String!): Device!
  updateDeviceRoom(deviceId: ID!, room: String!): Device!
}

input NewDeviceInput {
  firmware: String!
  id: Float!
  name: String!
  room: String
}

input PairDeviceInput {
  type: String!
}

type Query {
  device(deviceId: ID!): Device!
  devices: [Device!]!
  lastReading(deviceId: ID!): Reading
  lastWateredTime(deviceId: ID!): DateTime
  readings(date: String, deviceId: ID!): [Reading!]!
}

type Reading {
  battery_voltage: Float!
  device_id: ID!
  light: Float
  moisture: Float!
  temperature: Float!
  time: DateTime!
}
